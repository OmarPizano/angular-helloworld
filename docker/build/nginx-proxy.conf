upstream frontend {
    server SERVER_IP:8080;
}

upstream backend {
    server SERVER_IP:5000;
}

server {
    listen 80;
     
    location / {
        proxy_pass http://frontend;
    }    
    
    location /names {
        proxy_pass http://backend;
    }    
}
